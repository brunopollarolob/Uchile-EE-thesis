\chapter{Conclusions and future work}
\label{ch:conclusions}
This work successfully detailed the design and implementation of the F-engine for the \gls{charts} project, fulfilling all major objectives defined for the digital backend’s first stage.

The implementation integrates several innovations over the state of the art, including the use of a complex FFT for I/Q sampling and the demultiplexing of multiple antennas with a single ADC via the \gls{fdm} scheme. The system is capable of digitizing and channelizing up to 32 antenna signals across an input bandwidth of approximately 2.5 GHz. Regarding the specifics objectives declared at the beginning of this thesis, here are the achievements of each of them:

\begin{itemize}
    \item The implementation of a quadrature digitization scheme operating over a bandwidth of 2457.6 MHz, encompassing the full band delivered by the FDM stage.
    \item The digital generation of the four local oscillators required by the \gls{fdm} mixers using a single RFSoC DAC port, avoiding the need for external frequency synthesizers and providing real-time configurability. The LO generation module achieved SFDR of 39.31 dB, enough to operate the mixers in the FDM without significant leaks.
    \item The output spectra were successfully requantized to a compact 4+4 bit format after applying digital gains calibration. This enabled efficient data throughput while minimizing quantization bias.
    \item Development of a high-throughput packetizer that enables data transmission via a QSFP28 optical interface, transmitting data streams with a $\sim$5\% packet loss and discarding in between FDM chains channels.
\end{itemize}

Validation tests confirmed the integrity and coherence of the system. Laboratory tests, including the 8-antenna correlation test, validated the phase stability and effectiveness of the digital gain calibration procedure necessary for interferometric observations. Furthermore, the successful solar transit observation confirmed the expected interferometric fringe rates and demonstrated phase coherence across the full digital chain in an on-sky environment. The recovered projected baseline matched the nominal separation of 20 meters to within a few centimeters.

The results of this work validate the architectural approach of CHARTS and its F-engine component, providing a flexible, modular, and scalable foundation for the array to achieve its scientific objectives.

\section{Future work}

While the current F-engine meets its primary performance requirements, several developments are necessary to scale the system toward a full CHARTS deployment. The most critical areas involve board synchronization, timing infrastructure, and multi-board data routing for the X-engine.

\subsection{Integration with the X-engine and header formatting}

An important step for the next phase is the full integration of the F-engine output with the \texttt{kotekan} based X-engine. This requires ensuring compatibility in packet structure, metadata, and timing conventions, as well as adapting 512 bit UDP payloads to the high performance \texttt{DPDK} ingestion engine. Each packet header must therefore unambiguously convey the data origin and organization, provide precise timestamps for synchronization, and specify the data format so the receiver software can reconstruct the channel and antenna layout without ambiguity.

Table~\ref{tab:udp_packet_format} proposes a header layout that collects the minimum fields required to meet these needs; each field is explained below and its implementation should be tested jointly with the X-engine to verify interoperability and performance.

\begin{table}[htbp]
\centering
\begingroup
\footnotesize
\setlength{\tabcolsep}{6pt} % reduce column separation
\renewcommand{\arraystretch}{0.95} % slightly tighter rows
\begin{tabularx}{\linewidth}{@{}l c X@{}}
\toprule
Field & Size (bytes) & Description \\
\midrule
Sequence number & 8 & Incremental packet counter for loss detection and reordering. \\
Timestamp & 8 & Time of the first sample in the packet (IRIG/UTC). \\
Board ID & 1 & Identifier of the RFSoC board generating the packet (0–7). \\
Sub‑band ID & 1 & Identifier of the frequency portion carried by this stream (0–3). \\
Number of channels & 2 & Number of frequency channels included per chain (672). \\
Geometry ID & 32 & Index/table mapping channels to antennas and array topology. \\
Flipped chains & 1 & State bits indicating FDM chains that are flipped. \\
Payload & 5376 & Requantized spectrum data corresponding to~$\sim$50\,MHz of channels. \\
\bottomrule
\end{tabularx}
\endgroup
\caption[Proposed UDP packet format for F‑engine output]{Proposed UDP packet format for the F‑engine output to the X‑engine, including essential metadata fields and the data block.}
\label{tab:udp_packet_format}
\end{table}

The fields above, together with bytes reserved for alignment and future extensions, constitute the packet header; in the current proposal the header occupies 64 bytes in total and is followed by a 5376‑byte payload. Considering link headers (Ethernet/IP/UDP), the combined packet size is on the order of 5482 bytes. Finally, this structure must be validated with end-to-end tests to verify timestamp integrity, correct antenna assignment, and tolerance to packet loss or reordering.

\subsection{Synchronization across multiple RFSoC boards}

Scaling beyond a single F-engine requires tight timing synchronization between several RFSoC boards so that all ADCs sample with a common time and frequency reference. The planned approach is to use a GPS disciplined timing system, such as the TCG 01-G\footnote{\url{https://www.microchip.com/en-us/product/tcg_01-g}} or the SyncServer S650\footnote{\url{https://www.microchip.com/en-us/products/clock-and-timing/systems/gnss-timing-instruments/syncserver-s650}}, which provides:

\begin{itemize}
    \item a 10 MHz frequency reference,
    \item a 1 PPS timing pulse, and
    \item an \gls{irigb} stream.
\end{itemize}

The 10~MHz signal would discipline all RFSoC clocks, ensuring identical ADC sampling rates across boards. Achieving this requires modifying the configuration of the LMK04828 clock synthesizer: the device must be reprogrammed (via TI’s TICS PRO\footnote{\url{https://www.ti.com/tool/TICSPRO-SW}}) to use CLKIN0 as the primary reference input, replacing the current internal SI5395B clock routing through CLKIN1 (see Figure~\ref{fig:rfsoc_clocking_scheme}).

\begin{figure}[h!]
    \centering
    \includegraphics[width=\textwidth]{rfsoc_clocking_cheme.png}
    \caption[Default RFSoC 4x2 clocking scheme]{Default RFSoC 4x2 clocking scheme, where the SI5395B provides the reference clock to the LMK04828 synthesizer through CLKIN1. The proposed modification involves reconfiguring the LMK04828 to use an external 10~MHz reference connected to CLKIN0, ensuring synchronized sampling across multiple boards. Extracted from the RFSoC 4x2 datasheet \citep{RealDigital_RFSoC4x2_RefMan_A3}.}
    \label{fig:rfsoc_clocking_scheme}
\end{figure}

Using an approach similar to the timing system developed in \citet{jorquera2020voltimetro}, this IRIG decoded timestamp will be embedded at the start of every UDP packet as described in Table \ref{tab:udp_packet_format}, enabling high-precision timestamping of each spectrum frame. The 1 PPS signal will be used to align the internal frame counters of all RFSoC boards, ensuring that each board’s data frames are synchronized to within a few nanoseconds.

\subsection{Corner-turn expansion and multi-board networking}

A crucial point to emphasize is that each RFSoC board is only capable of digitizing up to 32 antennas. For the X‑engine to operate correctly, a second-stage corner turn is therefore required so that each outgoing packet contains channels from all 256 antennas. Concretely, this implies additional networking and aggregation logic: a high‑capacity switch (or dedicated backplane) must collect sub‑band streams from multiple RFSoC boards and reassemble them so that each backend node receives the full set of antennas for its assigned frequency slice. This requires packet aggregation logic that merges packets from distinct RFSoC sources into unified frames, preserves sequence numbers and timing, and handles reordering and loss recovery. The architecture and functionality would then be similar to the CHIME corner turn backplane \citep{CHIME_cornerturn_2016}, but adapted to the specific requirements and scale of CHARTS.
This multi-board networking and aggregation layer is essential for scaling the F-engine to the full CHARTS array, ensuring that the X-engine receives correctly formatted data streams for correlation and beamforming across all antennas.

